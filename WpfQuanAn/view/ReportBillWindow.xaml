<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfQuanAn.ViewModel"
        xmlns:Viewer="clr-namespace:SAPBusinessObjects.WPF.Viewer;assembly=SAPBusinessObjects.WPF.Viewer" 
        x:Class="WpfQuanAn.view.ReportBillWindow"
        mc:Ignorable="d"
        x:Name="RepoertWD"
      xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        
        DataContext="{StaticResource banHangVM}"
        Title="ReportBillWindow" Height="450" Width="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedWindowReportCommand}" 
                                   CommandParameter="{Binding ElementName=RepoertWD}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Viewer:CrystalReportsViewer x:Name="CrystalReportsViewer"
                                     local:ReportSourceBehaviour.ReportSource="{Binding Path=DataContext.Report, 
                                     RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=FrameworkElement}}"/>
    </Grid>
</Window>
